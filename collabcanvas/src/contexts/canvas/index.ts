/**
 * @fileoverview Barrel export for canvas context and provider
 * Provides clean import path for consumers
 */

export { CanvasProvider, useCanvasContext } from './CanvasProvider';
export type { CanvasContextType } from '../../utils/types';

